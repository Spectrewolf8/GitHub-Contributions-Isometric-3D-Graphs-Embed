isometric-contributions.zip
.DS_Store
*.un~
node_modules
.direnv
.envrc
src/Archive.zip
.do_tasks
dist
.parcel-cache
.claude
artifacts
.env
error-screenshot.png

# API Cache
.cache/*.png


# Local MCP server config (may contain project refs; managed by the app)
.mcp.json

# Local-only load test reports and harness
benchmarks/
